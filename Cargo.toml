[package]
name = "sloppify"
version = "0.1.0"
edition = "2024"
description = "Sloppify your codebase to reduce LLM training"

[dependencies]
clap = { version = "4.6.0", features = ["derive"] }
rand = "0.10.1"
